<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/each_person"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include
        android:id="@+id/chat_toolbarLayout"
        layout="@layout/toolbar"
        app:layout_constraintStart_toStartOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/chat_RCV"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@id/chat_send_BTN"
        app:layout_constraintTop_toBottomOf="@id/chat_toolbarLayout" />

    <androidx.constraintlayout.widget.Barrier
        android:id="@+id/text_box_barrier"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:barrierDirection="top"
        app:constraint_referenced_ids="chat_text_EDT, chat_send_BTN"/>

    <EditText
        android:id="@+id/chat_text_EDT"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="@string/digite_aqui"
        android:inputType="text"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"

        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/chat_send_BTN"
        app:layout_constraintStart_toStartOf="parent" />

    <ImageView
        android:id="@+id/chat_send_BTN"
        android:layout_width="70dp"
        android:layout_height="50dp"
        android:layout_marginTop="40dp"
        android:background="@color/colorAccent"
        android:padding="10dp"
        android:src="@drawable/ic_send"
        app:layout_constraintBottom_toBottomOf="@id/chat_text_EDT"
        app:layout_constraintEnd_toEndOf="parent" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/colorGrayDark"
        app:layout_constraintTop_toTopOf="@id/text_box_barrier" />

</androidx.constraintlayout.widget.ConstraintLayout>